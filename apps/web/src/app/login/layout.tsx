/**
 * The sign-in page stands outside the application shell: there is no
 * navigation to show yet, and no signed-in officer to put in the top bar.
 */
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
